import XCTest
@testable import design_system_sp

final class design_system_spTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(design_system_sp().text, "Hello, World!")
    }
}
